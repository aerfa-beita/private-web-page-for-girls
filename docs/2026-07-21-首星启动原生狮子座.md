# 2026-07-21 首星启动原生狮子座

## 目标

保留 `lion_background.js` 的原生流星、星野、镰刀星和透明狮子座流程；电影文字只作为它的前置仪式。

## 启动桥接

- 页面加载：只启动电影黑幕、居中文字和第一颗星，不启动狮子座模块。
- 首星点击：`cinematic-opening.js` 派发 `openingRitualFirstLight`。
- `bootstrap()` 的一次性监听器调用 `window.initLionBackground()`。
- 原生模块从零开始播放其原有 0–9.4 秒时间轴；仅保留经视觉验收的月亮构图参数调整，狮子座时间轴未改。
- 9.6 秒后显示星点密码。

## 视觉修正

- 前置文字以视口为基准居中，使用暖金渐变、衬线字体、柔光，以及一次性的星尘引线和模糊聚焦淡入。
- “轻轻点一下”移至首星下方、放大并加入两侧微星装饰。
- 首星点击约 1.15 秒后，前置文字和首星层隐藏，不与狮子座重合。
- 原生月亮放在狮头朝向之外的右上留白区（`470, 300, -250`）并缩小为 `0.68`；调整为低饱和香槟色、弱外晕，避免紧贴星座或显得突兀。开场期间隐藏额外的长按情书月亮按钮，避免双月亮。

## 缓存

- `lion_background.js?v=4`
- `cinematic-opening.js?v=7`
- Service Worker：`our-universe-v33-moon-composition`

## 验证

- `lion_background.js` 与主目录同名文件逐行一致。
- `node --check` 通过。
- `node --test tests/site-features.test.js` 通过。
