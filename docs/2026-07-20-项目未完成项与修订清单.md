# 2026-07-20 项目未完成项与修订清单

> 本文是当前项目的唯一收尾清单。它区分“已经具备代码”与“已经在线验证”，避免把本地修改误判为线上已完成。

## 结论先行

- `love.xiahuaaitalk.top`、Vercel、Firebase 匿名登录、Cloud Firestore 与 Storage 控制台均已建立；它们不再是创建层面的阻塞项。
- 留言板、开场、月亮、狮子座等功能已有主体代码，但还没有完成一次完整的线上验收。
- “返回星空后再次轻触无法打开密码页”的修复已在本地提交 `188d5a5`，但尚未推送到 GitHub/Vercel；因此线上仍可能运行旧版本，不能据此认定修复失败。
- 本轮只梳理和补全文档，不把未验证的功能标记为完成。

## 一、未完成的工作

### P0：先让线上版本与本地一致

| 事项 | 当前状态 | 需要做什么 | 验收标准 |
| --- | --- | --- | --- |
| 发布重复进入密码入口修复 | 本地提交已存在，线上未包含 | 在 GitHub 网络可用后推送 `188d5a5`，等待 Vercel 自动部署 | 页面版本更新后，连续“进入 → 返回 → 再进入”至少 3 次均可打开密码锁 |
| 线上开场回归 | 未完整验收 | 在实际域名测试 Three.js 正常与 CDN 失败两种路径 | 不出现只有空星空和按钮的页面；两种路径都有完整标题、月亮、狮子或备用视觉 |
| Service Worker 更新验证 | 未验收 | 发布后重新打开页面并检查是否取得新版缓存 | 不需要用户手动清除缓存才看到最新交互 |

### P1：Firebase 留言与图片能力

| 事项 | 当前状态 | 需要修改 | 涉及位置 |
| --- | --- | --- | --- |
| Firestore 留言端到端验收 | 匿名登录代码已接入；规则是否已发布、线上是否可读写仍需验证 | 发布 `messages` 生产规则后，发送一条文字留言并在另一设备确认实时出现 | Firebase 控制台、`index.html` |
| 留言失败状态 | 已有基础错误提示 | 把“加载中”设为可超时、可重试的状态，避免一直停留在加载文案 | `index.html` |
| 图片留言上传 | Storage 已可用，但前端上传 UI 与逻辑仍被注释 | 实现选择图片、预览、大小/类型限制、上传、写入图片 URL、展示和失败提示；同步收紧 Firestore/Storage 规则 | `index.html`、Firebase Storage、Firestore 规则 |
| 访客防滥用 | 未完成 | 为匿名留言增加长度限制、频率限制或服务端校验；上线 App Check 前不要把它当作强防护 | `index.html`、Firebase 控制台、后续服务端 |

### P2：真实内容资产上线

| 事项 | 当前状态 | 需要修改 | 说明 |
| --- | --- | --- | --- |
| 照片在线可见 | 本地照片目录被 Git 忽略，Vercel 不会随代码发布 | 选择 Firebase Storage 或 Vercel Blob 承载照片，并把 `photos[]` 改为线上 URL | 这是线上照片为空或不同设备看不到照片的根因 |
| 背景音乐在线可播 | 本地音乐同样被 Git 忽略，且当前是 `.flac` | 上传至对象存储并优先使用浏览器兼容性更好的 MP3/AAC；处理自动播放被浏览器拦截后的点击播放提示 | 不应依赖开发电脑上的本地文件 |
| 第 6 张照片与私密文案 | 待小花先生挑选 | 替换配置数组中的临时内容 | 只改内容，不改变功能结构 |
| 月亮专属情书 | 线上会回退到通用情话 | 在安全的配置/服务端内容源中提供 `MOON_LETTERS`，每次长按随机展示 | 私密文案不得提交到公开仓库 |

### P3：AI、天气与真正访问保护

| 事项 | 当前状态 | 需要修改 | 原因 |
| --- | --- | --- | --- |
| 星语小助手接入 Cyber-AI | 前端接口边界已留出，Cyber-AI 尚未调试完成 | 由同源 Vercel API/反向代理转发到 Cyber-AI；确认返回格式、超时、失败文案与限流 | 浏览器不能直接保存 AI 密钥或跨站调用私有接口 |
| 天气卡片 | 线上私有 `config.js` 不发布，天气 Key 不可用 | 改为同源服务端代理，或在未配置时隐藏天气卡并显示优雅的本地文案 | 不能把天气 Key 写进公开前端 |
| 密码的真实安全性 | 当前密码只控制前端 UI | 若要限制访问，改为 Vercel 服务端校验 + HttpOnly 会话 Cookie，并保护内容/接口 | 目前知道页面地址的人仍可通过浏览器开发工具绕过前端锁 |

### P4：视觉、交互与性能收尾

| 事项 | 需要修改 | 验收重点 |
| --- | --- | --- |
| 开场与狮子座节奏 | 实机核查“流星出现 → 主星连续点亮连线 → 狮子从暗金到完整显影 → 轻触入口”是否有跳帧或断线 | 动画不突兀，首屏一眼能看出狮子座，不像贴图覆盖 |
| 月亮长按 | 验证进度环、取消长按、重复长按、随机情书和移动端触摸冲突 | 长按有明确回馈；不误触、不重复弹窗 |
| 密码返回流程 | 在推送新版后复测重复进入；若仍复现，再改为可重置且只绑定一次的状态机 | 可重复进入、按钮始终可点、无叠加监听 |
| 小屏与弱网 | 检查 600px 以下布局、低性能设备粒子数、`prefers-reduced-motion` 和 CDN 失败备用开场 | 不遮挡、不卡死、弱网仍有完整入口 |
| 通用质感 | 清理剩余泛化 emoji、廉价渐变按钮、失败信息和空白卡片 | 保持深空、暖金、克制而有私人感的视觉语言 |

## 二、需要修改的文件清单

| 文件或系统 | 下一次修改目的 |
| --- | --- |
| `index.html` | 留言超时/重试、图片上传、天气降级、访问状态、移动端与交互收尾 |
| `lion_background.js` | 仅在上线复现后修复重复进入和开场帧节奏；保留已确认的狮子视觉 |
| `sw.js` | 每次用户可见功能发布时递增缓存版本并验证更新 |
| `ai_service.js` | Cyber-AI 调试完成后对接同源代理、失败与限流状态 |
| `firebase-config.js` | 仅保存公开 Firebase 网页配置；不放天气 Key、AI 密钥、私密情书 |
| `config.js` | 仅保留本地私密配置；线上功能若依赖它，必须替换为服务端或安全内容源 |
| Firebase Firestore/Storage 规则 | 随文字留言、图片上传的实际字段和访问权限同步调整 |
| `README.md`、`INDEX.md`、`STRUCTURE.md`、`CLAUDE.md`、`AGENTS.md` | 每项验收后同步状态，避免旧文档误导下一次维护 |

## 三、仓库整理结论

| 项目 | 结论 | 原因 |
| --- | --- | --- |
| `serve-local.js` | 保留 | 是本地预览服务器，应补入索引 |
| `流星雨.html` | 暂不删除 | 它未参与主站部署，可能是独立原型；删除前需要小花先生明确确认 |
| `.worktrees/` | 保留 | 是隔离开发草稿，不属于 Vercel 主部署源 |
| `assets/Music/`、`assets/Photograph/` | 保留在本地，但不视为线上资源 | 目录被 Git 忽略；后续应迁移而非直接删除 |
| `config.js` | 保留且继续忽略 | 可能包含私密配置，不能提交 |

## 四、推荐执行顺序

1. 解决 GitHub 网络后发布本地的密码入口修复，并以线上页面复测。
2. 发布 Firestore 规则，完成一次文字留言的双设备读写验收。
3. 实现图片上传并同步规则，完成上传、刷新、另一设备查看的验收。
4. 把照片与音乐从本地目录迁移到线上存储。
5. 接入调试完成后的 Cyber-AI 同源代理。
6. 决定是否把“前端密码”升级成真正的服务端访问保护。
7. 最后做移动端、弱网、视觉节奏与缓存更新的整体验收。

## 五、完成定义

只有同时满足以下条件，事项才可从本文移到“已完成”：

1. 代码或控制台配置已变更；
2. 已发布到 `love.xiahuaaitalk.top`；
3. 真实浏览器完成对应验收；
4. 相关文档、规则和缓存版本已同步。

