# 2026-07-22 放映机投稿可读性调整

## 变更

- 放映机投稿区的三个操作按钮在服务尚未就绪时仍保留完整文字与颜色，不再通过降低整体透明度制造“暗淡不可见”的效果。
- 按钮仍保留不可点击状态与 `not-allowed` 鼠标提示；这只阻止误操作，不再让页面后半段看起来像未完成的渐变区域。

## 留言保留说明

- Firestore 留言代码、已有测试数据与本地测试开关均继续保留。
- 公开页面默认不展示留言测试面板，目的是不向访客暴露服务状态，而不是删除留言功能或数据。
- 任何清理 `messages` 数据的操作仍需项目所有者单独确认。
