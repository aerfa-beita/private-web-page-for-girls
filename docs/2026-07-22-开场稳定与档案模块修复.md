# 2026-07-22 开场稳定与档案模块修复

## 本次完成

- 开场文字改为页面真正可见后才开始计时；取消逐字碎裂，避免手机或后台标签页出现首句闪过、缺字或乱码。
- 使用已确认的星图背景 `assets/backgrounds/celestial-atlas-background.png`，并纳入 Service Worker 缓存。
- 首页照片固定为六张纵向拼接的拍立得；底部旧导航完全移除。
- 新增独立「档案」模块：完整照片放映机、照片投稿入口与留言墙均移入该模块，左侧地图抽屉增加「04 档案」。
- 双人在线心跳改为粒子心形、扩散光环和粉色脉冲线；仍只在两位不同访客同时在线时显示。
- 背景音乐播放列表改为当前 `assets/Music/` 的真实 MP3 文件名。
- Service Worker 更新为 `our-universe-v45-atlas-background-opening`，以刷新开场脚本与背景资源。

## 暂不处理

- 地球主视觉、真实地球旋转与旅行地点录入仍保留到下一轮单独设计确认。

## 验证

- `node --check scripts/opening/cinematic-opening.js`
- `node --check scripts/ui/earth-atlas.js`
- `node --test tests/site-features.test.js`：28 项通过。
