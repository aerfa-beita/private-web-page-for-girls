# 未完成事项交接清单（2026-07-23）

本清单覆盖较早文档中的零散待办。没有列出的历史实验不再继续推进。

## 当前决定

- 朋友展示阶段继续由 Vercel 承载现有照片和音乐；不迁移、不删除现有媒体，也不因 Firebase 方案改动展示路径。
- 地球、心跳等视觉改造必须先提供设计图，经小花先生确认后再写 UI。
- 测试留言保留；正式交付前是否清空，必须由小花先生再次明确确认。

## P0：上线前必须完成

| 事项 | 当前状态 | 下一步 |
| --- | --- | --- |
| 发布 Firebase 规则 | `firebase/firestore.rules` 和 `firebase/storage.rules` 已写好并绑定 `new-univese`，尚未在控制台发布完成。 | 在 Firebase Console 发布两份规则；不删除照片、留言或任何数据库记录。 |
| Firebase 真实读写验收 | 代码有匿名登录、留言、双人心跳和投稿流程；未完成线上验证。 | 分别验证匿名留言、两台设备在线心跳、朋友照片压缩上传与每日限额。 |
| Vercel 正式发布 | 当前工作区有大量待审改动，尚未形成可安全覆盖线上版本的提交。 | 小花先生确认页面后，只挑选本次确认的代码、音乐和照片提交，再由 Vercel 部署。 |
| 多设备回归 | 开场首句、音乐文件名、档案入口均已修复代码，尚未完成线上多设备确认。 | 用手机和另一台电脑检查：首句完整停留、无乱码、音乐可播放、6 张照片和档案均可打开。 |

## P1：已写代码但仍待页面验收

| 事项 | 当前状态 | 验收重点 |
| --- | --- | --- |
| 开场首句稳定性 | 已改为可见页面后再开始、整句文本渲染。 | 首句不跳过、不乱码，切后台再回来不会乱序。 |
| 星图背景与纵向拍立得 | 已换为小花先生确认的星图背景，6 张改为纵向连续拍立得。 | 背景已生效；照片布局与当前线上旧版的连续样式一致。 |
| 独立档案模块 | 留言墙与完整放映机已移至“档案”，首次进入才初始化。 | 留言可见、气泡分页正常，放映机不会拖慢开场。 |
| 双人心跳 | 已加入粒子心形、光环与脉冲线。 | 只有两位不同访客同时在线时才显示完整效果，单人保持克制。 |

## P2：需先出设计图，再写代码

1. 地球主页面：替换现有 AI 感地球，采用常见、可信的地球视觉，并补上明确的旋转与展示节奏。
2. 旅行地点共建：小花先生与对方可新增去过的地点；每个地点包含位置、一两句话和照片。地球设计确认后再确定表单、审核与 Firebase 数据结构。
3. 心跳视觉最终版：当前代码只是参考图的第一版，视觉细节仍须先看设计图确认。
4. 隐藏信封：小花先生仍在考虑用途，暂不开发。

## 延后执行，当前不要动

| 事项 | 原因与边界 |
| --- | --- |
| 完整照片迁移 Firebase Storage | 当前先保证朋友展示稳定，照片继续从 Vercel 加载；等未来向更多人开放时再评估迁移。 |
| 删除旧照片、音乐或测试留言 | 都属于不可逆的数据操作；没有小花先生的单独确认不得执行。 |
| Cyber-AI 同源代理 | 未完成调试，不接入公开页面。 |

## 执行顺序

1. 发布 Firebase 规则并完成真实读写验收。
2. 完成当前版本的线上多设备验收。
3. 小花先生确认后，挑选改动发布至 GitHub/Vercel。
4. 只对地球、旅行共建、心跳视觉出设计图；确认后再继续写 UI。
