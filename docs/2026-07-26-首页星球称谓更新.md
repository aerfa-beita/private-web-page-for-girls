# 首页星球称谓更新

日期：2026-07-26

## 变更

密码解锁后默认显示的“第一束光”星球私语，第三行由“已经朝向小花先生。”更新为“已经朝向钰涵大人。”

文案位于 `scripts/ui/earth-atlas.js` 的 `GALAXY_PLANETS` 配置首项，并标记为 `【小花先生改行星文案与位置】`，后续可直接在此处调整。

## 缓存与验证

- 脚本版本更新为 `scripts/ui/earth-atlas.js?v=13`。
- Service Worker 缓存更新为 `our-universe-v69-yuhan-planet-copy`，确保手机重新打开页面后拉取新文案。
- 回归测试覆盖新称呼和旧称呼不存在的断言。
