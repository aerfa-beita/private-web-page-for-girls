# 2026-07-21 文字聚星与首星对齐

## 目标

删除开场左侧远处光点，使第一句字幕本身成为首星的来源；第二句与首星保持同一条垂直视觉轴。

## 实现

- `cinematic-opening.js` 继续复用现有字幕与首星元素，不新增开场模块。
- 第一段“有人，轻轻想起了你。”拆成字形片段；每个字在原位以两块镜面斜切短暂停顿，再沿实际屏幕坐标平滑汇聚至中心。没有弹跳、瀑布式逐字延迟或大范围外扩。
- 汇聚过程同步显示原有 `#cinematic-origin-star`，因此最终只留下同一个可点击首星。
- 移除 `is-distant-star` 时间点和样式，开场黑幕不再出现左侧远处光点。
- 第二段“于是，宇宙亮起了第一颗星。”改为单行，中心位于首星下方约 94px（移动端约 84px），不再落在页面下半区。

## 缓存与验证

- `scripts/opening/cinematic-opening.js?v=7`
- Service Worker：`our-universe-v37-mirror-caption`
- 验证：`node --check scripts/opening/cinematic-opening.js` 与 `node --test tests/site-features.test.js`。
