# 2026-07-21 主目录媒体部署清单

## 已核对的主目录素材

- `assets/Music/`：8 首 MP3，已写入 `MUSIC_PLAYLIST`，首次点击首星后随机播放并在解锁后延续。
- `assets/Photograph/IMG_20260228/`：93 张可展示图片，全部存在于 `image_carousel.js` 的放映机清单中。
- 同目录另有 DNG、MP4 与目录占位文件；它们不进入网页照片轮播，也不随网站部署。

## 发布规则

- `.gitignore` 允许 `assets/Music/*.mp3`。
- `.gitignore` 允许 `assets/Photograph/IMG_20260228/` 下的 JPG、JPEG、PNG。
- FLAC、DNG、视频、`config.js` 仍保持忽略，避免无效文件与私密配置进入部署。

## 主目录发布步骤

1. 合并 `codex/modular-opening` 至主目录，使播放列表与忽略规则生效。
2. 在主目录暂存 `assets/Music` 与 `assets/Photograph`。
3. 提交并推送 `main`；Vercel 自动部署后，线上页面直接使用这些相对路径媒体。

## 缓存

- Service Worker：`our-universe-v38-main-media-mp3`
