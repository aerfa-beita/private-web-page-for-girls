# 2026-07-19 仓库清理

## 清理范围

- 删除 3 个临时分析产物：`_tmp_build_ppt.py`、`_tmp_doc_analysis.json`、`_tmp_extract_docs.py`。
- 删除未被当前页面加载的旧开场实验：`intro.css`、`intro.js`、`star_awakening.js`。
- 删除未被页面引用的 3 张狮子座设计源图，仅保留运行时使用的 `assets/Leo/leo-linework-transparent.png`。

## 保留范围

- `assets/Music/`、`assets/Photograph/`、`assets/Video/`：私人媒体，继续由 `.gitignore` 排除。
- `config.js`：本地私密配置，继续不提交。
- `.worktrees/v4-leo-opening/`：Git 工作树，未做清理。
- `serve-local.js`：本地 5173 预览服务器，保留。

## 验证

- 页面运行时仅引用透明狮子线稿；无代码引用已删除的实验脚本与设计图。
- `node --check lion_background.js` 与 `node tests/site-features.test.js` 通过。
