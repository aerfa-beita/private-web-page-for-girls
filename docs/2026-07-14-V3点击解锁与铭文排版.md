# V3 点击解锁与铭文排版

## 体验调整

- 三段记忆改为纵向独立铭文：日期固定在左侧，文案在右侧，避免在窄屏和拖拽星图后发生重叠。
- 网页开场和狮子座动画完成后不再自动打开密码锁。
- 开场完成时显示“轻触星空，进入我们的宇宙”提示；用户首次点击或触摸后才显示密码输入框。
- Three.js 加载失败的 15 秒兜底路径也采用相同的点击解锁方式。

## 验证

```powershell
node --test tests/site-features.test.js
node --check lion_background.js
```
