# 2026-07-19 开场视觉退场

## 问题

开场狮子、月亮与 Three.js 画布采用 `position: fixed`，离开狮子座静观页后仍覆盖在照片和其他内容模块上。

## 修复

- 静观页的“走进我们的宇宙”触发 `mainExperienceEntered` 事件。
- `lion_background.js` 收到事件后淡出并移除开场标题、六星、透明狮子、月亮按钮和 Three.js 容器。
- 渲染循环在视觉退场后停止；照片、留言和轮播页不再承载狮子座背景。
- 当前开场脚本为 `lion_background.js?v=17`，缓存为 `our-universe-v17-guided-entry`。

## 验证

- `node --check lion_background.js`
- `node --test tests/site-features.test.js`
- `git diff --check`
- 本地流程：开场 → 密码 → 狮子座静观页 → 进入首页 → 滚动照片，确认没有狮子、月亮或开场画布残留。
